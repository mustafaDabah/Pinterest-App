import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    marginTop: 32,
    textAlign: 'left',
    width: '90%',
    marginBottom: 10,
  },
  subTitle: {
    textAlign: 'left',
    width: '90%',
    marginBottom: 32,
    textTransform: 'capitalize',
  },
});
